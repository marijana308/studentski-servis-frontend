import { AverageGradePipe } from './average-grade.pipe';

describe('AverageGradePipe', () => {
  it('create an instance', () => {
    const pipe = new AverageGradePipe();
    expect(pipe).toBeTruthy();
  });
});
